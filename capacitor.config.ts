import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.izakdvlpr.reactvitepwa',
  appName: 'react-vite-pwa',
  webDir: 'dist'
};

export default config;
